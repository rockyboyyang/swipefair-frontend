import React from "react";
import { BrowserRouter, Route, Switch } from "react-router-dom";

const AboutUsContainer = (props) => {
    return (
        <div className="right-container">
            Hello from AboutUsContainer
        </div>
    )
}

export default AboutUsContainer;
