import React from "react";
import { BrowserRouter, Route, Switch } from "react-router-dom";

const SwipeContainer = (props) => {
    return (
        <div className="center-container">
            Hello from SwipeContainer
        </div>
    )
};

export default SwipeContainer;
